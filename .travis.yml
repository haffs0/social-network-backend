sudo: false

language: node_js

node_js:
  - "stable"

services:
  - postgresql

addons:
  postgresql: "12"

notifications:
  email: false

install:
  - npm install

script:
   - npm run coverage

after_success:
  - npm run coveralls

